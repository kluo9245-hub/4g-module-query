# 4G模块选型工具 — 手动操作指导手册

> 适用于没有代码基础的用户，按步骤操作即可完成修改并同步到在线网页。

---

## 一、文件说明

工具包含以下核心文件，知道它们的作用就知道该改哪里：

| 文件 | 作用 | 什么时候需要改它 |
|------|------|----------------|
| `data.json` | 存储所有国家/地区及其频段数据 | 新增/删除国家、修改某国的频段 |
| `build.py` | 定义所有模块型号、频段、归属产品 | 新增/删除模块、修改模块产品型号 |
| `template.html` | 网页的外观和展示逻辑 | 一般不需要改，除非要改网页样式 |
| `index.html` | **自动生成**，不需要手动改 | 每次运行 `build.py` 后自动更新 |

> ⚠️ **重要**：`index.html` 是自动生成的，永远不要手动改它！

---

## 二、准备工作

用 **记事本** 或 **Notepad++**（推荐）打开文件，不要用 Word。

- 记事本：右键文件 → 打开方式 → 记事本
- Notepad++：去微软应用商店搜索安装，更友好

---

## 三、修改国家数据（data.json）

用记事本打开 `data.json`，内容格式如下：

```json
{
  "countries": [
    ["中国", "亚太", "FDD B1/B3/B5/B8 TDD B34/B38/B39/B40/B41", "综合评估说明文字"],
    ["日本", "亚太", "FDD B1/B3/B8/B11/B21 TDD B41", "..."],
    ...
  ]
}
```

每一行代表一个国家，四个部分用逗号分隔：

| 位置 | 说明 | 示例 |
|------|------|------|
| 第1个 | 国家/地区名称 | `"中国"` |
| 第2个 | 所属区域 | `"亚太"`、`"欧洲"`、`"北美"` 等 |
| 第3个 | 4G频段 | `"FDD B1/B3/B8 TDD B41"` |
| 第4个 | 综合评估说明 | 任意文字 |

### 新增国家

在 `"countries"` 的方括号 `[` `]` 里，**最后一个国家后面加逗号**，然后新增一行：

```json
["泰国", "亚太", "FDD B1/B3/B8 TDD B41", "东南亚常用频段..."]
```

> ⚠️ 注意：逗号是英文逗号 `,`，不是中文逗号 `，`

### 删除国家

找到对应国家的那一行，整行删除（包括结尾的逗号）。

### 修改频段

修改第3个字段，格式固定为 `FDD Bx/By TDD Bz`，频段之间用 `/` 分隔。

---

## 四、修改模块和产品型号（build.py）

用记事本打开 `build.py`，找到 `MODULES = {` 这一段（大约在文件第28行）。

### 模块结构说明

每个模块长这样：

```python
'CN': {                                           # ← 模块编号（不要改）
    'name': 'MC669-CN / LE270-CN / ...',           # ← 模块完整名称
    'short_name': 'CN三合一',                       # ← 简称
    'bands': {'FDD': ['B1','B3','B5','B8'],
              'TDD': ['B34','B38','B39','B40','B41']},
    'has_gsm': False,                              # ← True=支持GSM，False=不支持
    'priority': 100,                               # ← 数字越大越优先展示
    'region': '亚太/中东/非洲/大洋洲',              # ← 适用区域
    'note': '不支持GSM，适合亚太地区',              # ← 备注说明
    'projects': {'MC669-CN': ['WD-219G'],
                 'LE270-CN': ['WD-300','WD-210']} # ← 产品型号（见下方详解）
}
```

### 修改产品型号（projects 字段）

`projects` 有两种格式：

**格式A：简单列表**（一个模块对应多个产品）
```python
'projects': ['WD-300', 'WD-210', 'WD-219K']
```

**格式B：按子型号分组**（CN三合一、EU版用这种）
```python
'projects': {
    'MC669-CN': ['WD-219G'],
    'LE270-CN': ['WD-300', 'WD-210', 'WD-218', 'WD-219K'],
    'ML307N-DC-DL': ['WD-110', 'WD-281', 'WD-282', 'MW-100', 'WD-280D']
}
```

- 新增产品型号：在列表里加 `'产品名',`（注意逗号）
- 删除产品型号：把对应 `'产品名'` 整行删掉（包括逗号）

### 新增模块

在 `MODULES = {` 和最后一个 `}` 之间，复制一个现有模块的代码块，修改对应内容，注意：
- 新模块的开头键名（如 `'NEW'`）不能和现有模块重复
- 每个模块之间用逗号 `,` 分隔
- `priority` 数字越大排在越前面

### 删除模块

找到要删除的那个模块的代码块（从开头键名到结尾的 `},`），整块删除，注意前后的逗号要处理干净。

---

## 五、生成网页（运行 build.py）

改完 `data.json` 或 `build.py` 之后，必须运行 `build.py` 重新生成 `index.html`。

### 方法一：双击运行（推荐）

1. 双击 `build.py` 文件
2. 会弹出一个黑色窗口，显示类似内容：
   ```
   ✅ 从 data.json 读取 171 个国家
   📊 自动匹配完成：共 821 条国家-模块关联
   ✨ 构建完成！
   ```
3. 看到 `✨ 构建完成！` 就说明成功
4. 按任意键关闭窗口

> 如果双击没有反应，用方法二。

### 方法二：命令行运行

1. 按 `Win + R`，输入 `cmd`，回车
2. 输入以下命令（路径根据实际情况调整）：
   ```
   "C:\Users\NING MEI\.workbuddy\binaries\python\versions\3.13.12\python.exe" "C:\Users\NING MEI\WorkBuddy\20260420143552\4g_module_tool\build.py"
   ```
3. 看到 `✨ 构建完成！` 就说明成功

---

## 六、推送到 GitHub（让在线网页更新）

> 只有完成这步，别人访问你的在线网页才能看到最新内容。

### 方法一：用 GitHub Desktop（最简单，推荐）

1. 去 https://desktop.github.com/ 下载安装 **GitHub Desktop**
2. 安装后登录你的 GitHub 账号
3. 找到 `4g-module-query` 仓库，点击 Clone
4. 以后每次修改完文件，GitHub Desktop 会自动检测到变化
5. 在左下角填写 **Summary**（随便写，如"更新国家数据"）
6. 点击 **Commit to main** 按钮
7. 再点击右上角 **Push origin** 按钮
8. 等待几分钟，在线网页自动更新

### 方法二：用命令行（已配置好，直接复制粘贴）

1. 打开 `4g_module_tool` 文件夹
2. 在文件夹地址栏输入 `cmd` 回车，打开命令行
3. 依次输入以下命令：

```bash
# 第1步：查看哪些文件被修改了（确认一下）
git status

# 第2步：把所有修改的文件加入提交（复制这一整行）
git add data.json build.py template.html index.html

# 第3步：提交（引号里写本次修改的说明，可自定义）
git commit -m "更新数据"

# 第4步：推送到GitHub（最关键的一步）
git push
```

4. 看到类似 `main -> main` 的字样就说明推送成功
5. 等待 1-2 分钟，访问 https://kluo9245-hub.github.io/4g-module-query/ 就能看到更新

---

## 七、常见问题

### Q：运行 build.py 报错怎么办？
A：检查 `data.json` 是否有格式错误，最常见的是少了逗号或多了逗号。把报错截图发给我。

### Q：git push 需要输入用户名密码怎么办？
A：说明你还没有配置 GitHub 凭证，告诉我，我帮你配置。

### Q：我不小心改错了文件怎么办？
A：右键文件 → 以前的版本（如果有备份），或者告诉我，我可以帮你恢复。

### Q：推送后网页没有立即更新？
A：GitHub Pages 需要 1-3 分钟部署，稍等再刷新页面（Ctrl+F5 强制刷新）。

---

## 八、操作检查清单

每次修改后，按这个顺序检查：

- [ ] 修改了 `data.json` 或 `build.py`
- [ ] 运行了 `build.py` 并看到 `✨ 构建完成！`
- [ ] 双击 `index.html` 打开本地网页，确认内容正确
- [ ] 执行了 `git push` 推送到 GitHub
- [ ] 等待 1-3 分钟后在线网页已更新
